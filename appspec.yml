version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/client
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:

  ApplicationStart:
    - location: ./deploy_client.sh
      timeout: 120
      runas: ubuntu